name: Flutter iOS Simulator IPA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-ios-simulator:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Recreate iOS project
        run: |
          rm -rf ios
          flutter create . --platforms=ios
          flutter pub get

      - name: Safe Podfile Injection
        run: |
          cd ios
          # غادي نخدمو بـ sed باش نزيدو الإعدادات ديالنا لداخل بلا ما نخسرو الـ Syntax
          # هاد السطر كيدخل وسط الـ post_install اللي ديجا كاين وكيزيد فيه الأوامر ديالنا
          sed -i '' '/target.build_configurations.each do |config|/a \
                config.build_settings["IPHONEOS_DEPLOYMENT_TARGET"] = "13.0" \
                config.build_settings["ENABLE_BITCODE"] = "NO" \
                config.build_settings["CODE_SIGNING_ALLOWED"] = "NO" \
                config.build_settings["CODE_SIGNING_REQUIRED"] = "NO" \
                config.build_settings["ONLY_ACTIVE_ARCH"] = "YES" ' Podfile
          
          # تثبيت المكتبات (Firebase, etc)
          pod install
          cd ..

      - name: Build iOS Simulator
        run: |
          flutter build ios --simulator --debug --no-codesign

      - name: Create IPA for Appetize
        run: |
          rm -rf Payload app-debug.ipa
          mkdir -p Payload
          # تأكد من المسار الصحيح
          if [ -d "build/ios/iphonesimulator/Runner.app" ]; then
            cp -r build/ios/iphonesimulator/Runner.app Payload
          else
            cp -r build/ios/Debug-iphonesimulator/Runner.app Payload
          fi
          zip -r app-debug.ipa Payload

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-simulator-ipa
          path: app-debug.ipa
